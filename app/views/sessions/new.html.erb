<script>
		alert("Usuário administrador:\n\tlogin: admin\n\tsenha: admin\n\nUsuário comum:\n\tlogin: user1\n\tsenha: user1\n\nCaso seja criado um novo usuário, esse será um usuário comum, a menos que a flag 'role' seja alterada no banco de dados de 'user' para 'admin'.");
</script>
<br>
<div class="wrap--sign">
<%= form_for :session, url: sign_in_path do |f| %>
    <div class="field field--sign">
      <%= f.label :login %><br />
      <%= f.text_field :login, autofocus: true %>
    </div>
   
    <div class="field field--sign">
      <%= f.label :password %><br />
      <%= f.password_field :password, autocomplete: "off" %>
    </div>
    <div class="actions actions--sign">
      <%= f.submit "Log in" %>
    </div>
  <p>New user? <%= link_to "Sign up now!", new_user_path %></p>
  <% end %>
</div>
